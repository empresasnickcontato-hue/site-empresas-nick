# URL do backend — OBRIGATÓRIA no build de produção (Render/Vercel).
# Sem ela o frontend cai no fallback do código. Configure como env de BUILD.
VITE_API_URL=https://site-empresas-nick-backend.onrender.com
VITE_SUPABASE_URL=sua_url_supabase
VITE_SUPABASE_ANON_KEY=sua_key
VITE_MY_WHATSAPP_API_URL=https://sua-evolution.com/message/sendText/sua-instancia
VITE_MY_WHATSAPP_API_KEY=sua_apikey_aqui
VITE_OWNER_PHONE=55DDDSEUNUMERO
# Email via Gmail SMTP (nodemailer) — backend/.env
# EMAIL_PASS = senha de app do Gmail com 16 caracteres (Conta Google > Segurança > Senhas de app)
